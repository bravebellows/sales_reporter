<%= form_with(model: csv_import, local: true) do |form| %>
  <% if csv_import.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(csv_import.errors.count, "error") %> prohibited this csv_import from being saved:</h2>

      <ul>
        <% csv_import.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :filename %>
    <%= form.file_field :filename %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
